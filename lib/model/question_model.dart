/// Question class
class Question {
  /// Named constructor
  Question.name(this.questionText, {this.isCorrect});

  /// Text for the question.
  String questionText;

  /// Is the question .
  bool isCorrect;
}
